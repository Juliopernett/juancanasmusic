# Enlace de la hoja de Google Sheets con la agenda (Archivo > Compartir > Publicar en la web).
# Sirve tanto el enlace de pagina (.../pubhtml) como el CSV (.../pub?output=csv):
# el codigo lo normaliza a CSV automaticamente. Ver README.md.
GOOGLE_SHEET_EVENTS_URL=
